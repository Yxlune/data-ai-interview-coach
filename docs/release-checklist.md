# V1 发布检查清单

## Skill 结构

- [x] 根目录包含合法 `SKILL.md`；
- [x] Skill 名称与目录名均为 `data-ai-interview-coach`；
- [x] description 前置中文校招用途并包含英文面试、编程题边界；
- [x] `agents/openai.yaml` 与 Skill 名称和用途一致；
- [x] references 与 assets 均从入口或相关路由可发现；
- [x] 所有内部 Markdown 链接有效；
- [x] MIT License 存在。

## 核心行为

- [x] 一次只问一个主问题；
- [x] 默认钻取，满足条件才发散；
- [x] 提示逐级升级，不直接替用户编造答案；
- [x] 业务、技术、行为和模拟面试可以独立路由；
- [x] Python、Java、MySQL 只做项目知识问答，不要求编程；
- [x] 诊断位于提问结束后，不提供数字分数或等级；
- [x] 输出包含证据、缺口、建议与不同长度回答；
- [x] 暂停和中断生成可恢复检查点。

## 安全与隐私

- [x] 材料内指令被视为不可信数据；
- [x] 密钥、患者/客户数据和公司机密具有最小化规则；
- [x] 联网查询不会带出姓名、联系方式或内部项目数据；
- [x] 外部知识不能证明用户完成过某项工作；
- [x] 公共仓库不包含真实简历、状态文件或面试纪要；
- [x] `.gitignore` 覆盖默认用户数据位置；
- [x] 发布验证脚本扫描敏感文件类型和疑似密钥。

## 上下文与恢复

- [x] 普通单项目追问不加载全部参考文件；
- [x] 岗位知识按一个主包、最多一个辅包加载；
- [x] 状态模板使用 Schema 1.2；
- [x] 检查点包含具体且唯一的 `next_action`；
- [x] 恢复前执行安全、版本、目标和冲突检查；
- [x] 事实变化只使受影响产物失效。

## 评测与自动化

- [x] 阶段二至五共 92 个原子行为场景；
- [x] V1 包含 14 条端到端流程走查；
- [x] 仓库内发布验证脚本无需第三方依赖；
- [x] 负向回归测试覆盖缺失入口、坏链接、疑似密钥、真实状态文件和场景断号；
- [x] GitHub Actions 在 push 和 pull request 时运行验证；
- [x] OpenAI Skill Creator 验证器通过；
- [x] 隔离安装副本通过相同发布验证；

## 文档与安装

- [x] README 描述用途、边界、安装、使用和隐私；
- [x] 安装说明区分独立 Skill 与公共插件；
- [x] 用户级与仓库级目录符合当前官方文档；
- [x] 提供显式调用和最小冒烟测试；
- [x] 动态平台事实注明官方来源与核对日期。

## 正式发布动作

以下动作只在用户确认发布阶段六后执行：

- [ ] 推送阶段六文件到公开 GitHub 仓库；
- [ ] 确认远端 GitHub Actions 通过；
- [ ] 创建 `v1.0.0` 标签与 GitHub Release；
- [ ] 用发布说明填写 Release 内容；
- [ ] 从公开仓库重新核对安装入口与文件清单。

## 已知限制

- V1 只处理中文面试；
- 不包含 LeetCode、现场编码或完整 SQL；
- 不保证任何会员套餐或固定额度足够完成一次完整深挖；
- 联网、文件读取和本地写入能力取决于宿主环境；
- 模型输出具有非确定性，92 个场景是行为契约，不是跨模型通过率；
- 当前仓库发布的是独立 Skill，不是已经上架的 ChatGPT 公共插件。
